fastapi
uvicorn[standard]
python-multipart
pytesseract
pillow
httpx
ultralytics
# Opsional: hanya dibutuhkan saat MODERASI_VISUAL=clip (CLIP zero-shot) atau =smolvlm (SmolVLM2-256M).
# Install via: pip install transformers num2words
# transformers
# num2words
# Opsional: torchvision untuk MODERASI_VISUAL=mobilenetv3 (sudah terinstall di deploy step 4).
# torchvision
